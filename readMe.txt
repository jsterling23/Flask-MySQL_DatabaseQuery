
Uses Flask, MySQL, python etc

This app is designed to add users to a MySQL database by their first name, last name, email and time stamps. 

This app also allows the user to edit/update the data base via local host and also to delete user data.